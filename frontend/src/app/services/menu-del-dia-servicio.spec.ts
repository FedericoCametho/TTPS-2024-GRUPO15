import { MenuDelDiaServicio } from './menu-del-dia-servicio';

describe('MenuDelDiaServicio', () => {
  it('should create an instance', () => {
    expect(new MenuDelDiaServicio()).toBeTruthy();
  });
});
